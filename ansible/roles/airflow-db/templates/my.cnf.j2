#
# The MySQL database server configuration file.
#
# You can copy this to one of:
# - "/etc/mysql/my.cnf" to set global options,
# - "~/.my.cnf" to set user-specific options.
# 
# One can use all long options that the program su>
# Run program with --help to get a list of availab>
# --print-defaults to see which it would actually >
#
# For explanations see
# http://dev.mysql.com/doc/mysql/en/server-system->

#
# * IMPORTANT: Additional settings that can overri>
#   The files must end with '.cnf', otherwise they>
#

!includedir /etc/mysql/conf.d/
!includedir /etc/mysql/mysql.conf.d/

[mysqld]
max_allowed_packet=1073741824
net_buffer_length=1000000

innodb_redo_log_capacity=1073741824